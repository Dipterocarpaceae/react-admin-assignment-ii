import Button from "@mui/material/Button";
import { styled } from "@mui/material/styles";

const CustomizedButtonColor = styled(Button)({
  color: "black",
  fontWeight: "bold",
});

export default CustomizedButtonColor;
